# VPS 限定部署授权

- Goal ID：GOAL-READING-WORLD-V1
- 决定 ID：DEC-15
- 动作 ID：ACT-08
- 授权时间：2026-08-13

## 用户原文事实

用户明确说明有远程 VPS 可用且已配置 SSH 免密，目标为 `root@tx.guyue.site`；允许在项目完成后部署一份。同时明确警告该主机已经由宝塔面板管理并部署了多个服务，必须注意对外暴露与 Web 服务代理，不得影响现有服务。

## 授权范围

只有本地发布候选、自动门禁与人工体验检查通过后，才允许执行以下动作：

1. 连接 `root@tx.guyue.site`。
2. 先只读盘点宝塔管理的站点、监听端口、反向代理、证书、磁盘与已有服务。
3. 在独立目录和独立内部端口部署本项目。
4. 在不覆盖、不停止、不改写现有服务的前提下配置独立反向代理。

## 明确禁止与重新确认条件

- 当前 PHASE-03 至 PHASE-08 不连接、不探测、不写入该 VPS。
- 不停止、不覆盖、不重启无关既有服务。
- 不复用已占用端口，不擅自开放额外公网端口。
- 不改写既有站点、域名、证书或宝塔全局配置。
- 不迁移或删除真实用户数据，不执行破坏性数据库动作。
- 如果独立域名/证书/目录/端口无法安全确定，或部署必须触及任何上述禁止项，立即暂停并请求用户确认。

## 结论边界

本证据只证明用户对上述条件化 VPS 部署动作作出授权；不证明项目已完成、发布候选已通过、VPS 已检查或任何部署已经发生。
